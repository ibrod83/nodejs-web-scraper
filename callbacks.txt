CollectContent, OpenLinks, DownloadContent:

function getElementList(elementList)){}
called in Operation.createElementList();
----------------------------------------------

OpenLinks:

function afterOneLinkScrape(cleanData){}
called in CompositeOperation.processOneScrapingObject()

function beforeOneLinkScrape(response){}
called in CompositeOperation.processOneScrapingObject()

All Operations besides root
function afterScrape(currentWrapper){}
called in scrape()

CollectContent
function processElementContent(content){}
called in scrape();

